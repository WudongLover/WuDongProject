import { Middleware, Inject, Provide } from '@midwayjs/core'; import { Context, NextFunction } from '@midwayjs/koa'; import { AuthService } from '../modules/user/service/auth_service';
@Provide() @Middleware() export class AuthMiddleware { @Inject() auth:AuthService; resolve(){return async(ctx:Context,next:NextFunction)=>{const p=ctx.path; if(p.startsWith('/api/user')&&['/api/user/me','/api/user/profile'].includes(p)||ctx.method!=='GET'&&p.startsWith('/api/app/m3')||ctx.method!=='GET'&&p.startsWith('/api/app/m4')){const h=ctx.headers.authorization||'';const id=this.auth.verify(h.replace(/^Bearer\s+/i,''));if(!id){ctx.status=401;ctx.body={code:1001,message:'未登录'};return;} (ctx as any).userId=id;} await next();};}}
