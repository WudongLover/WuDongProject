import { Column, CreateDateColumn, Entity, PrimaryGeneratedColumn } from 'typeorm';
@Entity('wudong_common_refresh_token') export class RefreshTokenEntity { @PrimaryGeneratedColumn({type:'bigint',unsigned:true}) id:string; @Column({name:'user_id',type:'bigint'}) userId:string; @Column({length:64,unique:true}) jti:string; @Column({name:'token_hash',length:128}) tokenHash:string; @Column({name:'expires_at',type:'datetime'}) expiresAt:Date; @Column({name:'revoked_at',type:'datetime',nullable:true}) revokedAt:Date|null; @CreateDateColumn({name:'created_at',type:'datetime'}) createdAt:Date; }
