import { Column, CreateDateColumn, Entity, Index, PrimaryGeneratedColumn, UpdateDateColumn } from 'typeorm';
const decimalTransformer = { to: (v: number) => v, from: (v: string | null) => (v === null ? null : Number(v)) };
@Entity('wudong_m3_room_calendar') @Index(['roomTypeId','date'],{unique:true}) export class RoomCalendarEntity { @PrimaryGeneratedColumn({type:'bigint',unsigned:true}) id:string; @Column({name:'room_type_id',type:'bigint'}) roomTypeId:string; @Column({type:'date'}) date:string; @Column({type:'int',default:0}) stock:number; @Column({name:'price_delta',type:'decimal',precision:10,scale:2,default:0,transformer:decimalTransformer}) priceDelta:number; @Column({default:0}) closed:number; @CreateDateColumn({name:'created_at',type:'datetime'}) createdAt:Date; @UpdateDateColumn({name:'updated_at',type:'datetime'}) updatedAt:Date; }
