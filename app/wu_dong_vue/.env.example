# C 端前端环境变量（复制为 .env 后按需修改，.env 已 gitignore）

# ===== 阿里云 OSS 图片访问 =====
# OSS 访问基地址：https://{bucket}.oss-cn-xxx.aliyuncs.com 或绑定的自定义域名（如 https://img.example.com）
# 留空则静态配图回退仓库内 public/images 本地文件，页面不依赖 OSS 也能正常显示
VITE_OSS_BASE_URL=
# OSS 对象键前缀，需与后端 .env 的 OSS_PREFIX 一致
VITE_OSS_PREFIX=wudong
